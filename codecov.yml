ignore:
  - "rhizomedb/tests"
  - "rhizomedb-wasm"
  - "rhizomedb-benches"
  - "examples"

comment:
  layout: "reach, diff, flags, files"
  require_changes: true

github_checks:
  annotations: false

coverage:
  status:
    project:
      default:
        threshold: 5%
